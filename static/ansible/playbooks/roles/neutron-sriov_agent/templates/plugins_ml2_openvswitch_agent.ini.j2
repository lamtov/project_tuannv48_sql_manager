[DEFAULT]

[agent]
tunnel_types = vxlan
l2_population = True

[ovs]
bridge_mappings =  vlan_net:br-vlan,flat_net:br-flat
local_ip = {{ host_info.ip[host_name] }}
vhostuser_socket_dir = /run/openvswitch/
datapath_type = netdev
tunnel_bridge = br-tun

[securitygroup]
firewall_driver = iptables_hybrid

[xenapi]
